const App = () => {
    return (
        <div>
            <h1 className="text-3xl font-bold text-slate-600 underline">
                Laravel + Vite + React + Tailwind CSS
            </h1>
        </div>
    );
};

export default App;
